package com.yuyan.imemodule.keyboard.gesture

/** Lifecycle of the gesture selected by one primary touch session. */
enum class GestureLifecycle {
    UNDECIDED,
    DISCRETE_CONSUMED,
    CONTINUOUS,
}
